@model IEnumerable<Company.Data.Data.Shop.Category>
<nav class="light-blue lighten-1" role="navigation">
    <div class="nav-wrapper container">
        <a id="logo-container" href="#" class="brand-logo">Company!</a>
        <ul class="right hide-on-med-and-down">
            @foreach (var item in Model)
            {
                <li>@Html.ActionLink(item.Name, "Index", "Shop", new { id = item.IdCategory }, null)</li>
            }
            <li>@Html.ActionLink("Home page", "Index", "Home")</li>
            @* <li>@Html.ActionLink("Dicounts", "Discounts", "Home")</li> *@
            @* <li> *@
            @*     <a class="nav-link text-dark" asp-controller="Cart" asp-action="Index">Cart</a> *@
            @* </li> *@
        </ul>

        <ul id="nav-mobile" class="sidenav">
            @foreach (var item in Model)
            {
                <li>@Html.ActionLink(item.Name, "Index", "Shop", new { id = item.IdCategory }, null)</li>
            }
            <li>@Html.ActionLink("Strona główna", "Index", "Home")</li>
            @* <li>@Html.ActionLink("Dicounts", "Discounts", "Home")</li>  *@
            @* <li> *@
            @*     <a class="nav-link text-dark" asp-controller="Cart" asp-action="Index">Cart</a> *@
            @* </li> *@
        </ul>
        <a href="#" data-target="nav-mobile" class="sidenav-trigger"><i class="material-icons">menu</i></a>
    </div>
</nav>